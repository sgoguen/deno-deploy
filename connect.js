
// const locationUrl = "ws://" + location.host;
// console.log({ locationUrl });
// const ws = new WebSocket(locationUrl);
// ws.onmessage = function (e) { pre.textContent += e.data + "\n" }

function sendToServer(event) {
    // event.key == 'Enter' && ws.send(this.value);
}